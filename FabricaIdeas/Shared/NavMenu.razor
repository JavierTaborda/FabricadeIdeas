<div class=" navbar">
    <div class="container-fluid">
        
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column ">
        <div class="colorletra nav-item px-3 " >
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="ideas">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Registra tus ideas
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="listaideas">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Consultar
            </NavLink>
        </div>
    </nav>
     <div class="powered align-items-center justify-content-center " style="   padding-left: 10px; position:fixed; display: flex; align-items: center; ;bottom:0px;">
        <small class="text-muted"> Powered By  </small><img src="./images/LogoNeo.png" alt="Neo" width="60px" />
    </div>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
